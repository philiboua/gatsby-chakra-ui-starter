import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks"
import logoCompany from "@images/logo-1.svg"
import { Footer } from "@src/components"

<Meta title="Components/Footer" component={Footer} />

# Footer

`Footer` component is used to display social media links, sitemap, and legal content. The component is using the component `Slice` to define its content.

<ArgsTable of={Footer} />
