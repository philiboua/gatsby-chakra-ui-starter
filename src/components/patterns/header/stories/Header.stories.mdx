import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks"
import { Header } from "@src/components"
import logoCompany from "@images/logo-1.svg"
import { navigation } from "./content/data.js"

<Meta title="Components/patterns/Header" component={Header} />

# Header

`Header` component is used to display the navigation of the website and the logo of the company

## ** Preview **

### Header without navigation

<Canvas>
  <Story name="Header">
    <Header logo={logoCompany} />
  </Story>
</Canvas>

### Header with a navigation

<Canvas>
  <Story name="Header with navigation">
    <Header logo={logoCompany} content={navigation} />
  </Story>
</Canvas>

<ArgsTable of={Header} />
