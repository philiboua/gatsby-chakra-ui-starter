import { Meda, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks"
import { CardContainer } from "@src/components"
import * as cards from "./content/data.js"

<Meta title="Components/patterns/Cards" component={CardContainer} />

# Card container

`CardContainer` component is used to display multiple cards on the interface.

## ** Preview **

### Cards with Icons

<Canvas>
  <Story name="Cards with Icon">
    <CardContainer content={cards.withIcons} />
  </Story>
</Canvas>

### Cards with Images

<Canvas>
  <Story name="Cards with Images">
    <CardContainer content={cards.withImages} />
  </Story>
</Canvas>

<ArgsTable of={CardContainer} />
